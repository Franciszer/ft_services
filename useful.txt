Wordpress:
	root:password
DATABASE:
	root:password
FTPS:
	admin:password
INFLUXDB:
	admin:admin
	user:password

ssh admin@$(minikube ip) -p 4000
kubectl exec -it $(kubectl get pods | grep mysql | cut -d" " -f1) -- /bin/sh -c "kill 1"

todo:
	- remove NodePorts to user Meatallb with LoadBalancers
	- create custom INFLUXDB container
	- redo ftps container
	- add livenessProbe (to restart a container in case one of its processes is stopped)
	- tweak phpmyadmin deployment